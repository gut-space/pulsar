-e git+https://github.com/gut-space/analog-noise-estimator.git#egg=analog-noise-estimator

# The following packages will likely be needed soon. But for the time being, let's
# keep them commented out.

# Required for reading image files and generating charts
matplotlib

# Required for reading the image files in a sane way
python-opencv

#orbit-predictor
#DateTimeRange
#typing-extensions
#python-dateutil
